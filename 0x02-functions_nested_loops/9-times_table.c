#include "main.h"

/**
 * print_last_digit - print the 9 times table starting with 0
 * 
 * Return: unnecessary as char type in prototype is void
 */

void times_table(void)
{
	int a = 0;
	int b = 0;
	int c = 1;

	while (a <= 9)
	{
		while (b <= 9)
		{
			b = b * c
			-putchar(b + '0')
			b++
		}

}
