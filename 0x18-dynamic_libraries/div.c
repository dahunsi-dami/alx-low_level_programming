#include "cops.h"
/**
 * div - function that divides int a by b.
 * @a: the 1st integer to be divided.
 * @b: the 2nd integer to perform division with.
 * Return: the division of a & b.
 */
int div(int a, int b)
{
	int d;

	d = a / b;
	return (d);
}
