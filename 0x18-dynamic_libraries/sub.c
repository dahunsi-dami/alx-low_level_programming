#include "cops.h"
/**
 * sub - function that subtracts integer b from a.
 * @a: the 1st integer to be subtracted from.
 * @b: the 2nd integer to be subtracted.
 * Return: the difference of a & b.
 */
int sub(int a, int b)
{
	int diff;

	diff = a - b;
	return (diff);
}
